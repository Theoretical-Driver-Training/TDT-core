spring:
  application:
    name: TDT-core
  datasource:
    url: jdbc:postgresql://localhost:5432/tdt
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true